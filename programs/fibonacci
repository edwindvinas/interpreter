var a := 0;
var b := 1;

sub f (n) begin
    var c := 0;
    loop for n begin
        if n <= 0 then begin
            var z := n;
        end else begin
            var z := a + b;
            var a := b;
            var b := z;
        end

        var c := c + 1;
    end
end


print f(10);